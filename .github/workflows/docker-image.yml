name: Starting jenkins CI

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Build the Docker image
      run: ping 192.168.33.102
    - name: test
      run: cat /etc/hosts
